// Optional JS fallback content.
// Keep these empty for normal operation; primary content lives in /content/*.json.

// Hero slides are managed via JSON (content/hero.json).
// Leave this empty; homepage ignores it by default.
window.HERO_SLIDES = [];

// Upcoming events fallback (used only when content/upcoming.json is missing/empty)
window.UPCOMING_EVENTS = [];
